- name: 'Solar Production'
  unique_id: solar_production_watt
  unit_of_measurement: 'W'
  icon: >
    mdi:solar-power
  state: >
    {{ ((states('sensor.power_produced') | float(default=0)) * 1000) | int(default=0) }}
